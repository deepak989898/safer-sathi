import { FlightReviewClient } from "@/components/flights/flight-review-client";

export const metadata = {
  title: "Review Flight | Safar Sathi",
  description: "Review your selected flight fare before booking",
};

export default function FlightReviewPage() {
  return <FlightReviewClient />;
}
